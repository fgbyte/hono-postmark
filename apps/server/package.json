{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@hono-postmark/auth": "workspace:*",
    "@hono-postmark/db": "workspace:*",
    "@hono-postmark/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "^4.8.2",
    "postmark": "^4.0.5",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@hono-postmark/config": "workspace:*",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "@vitest/ui": "catalog:",
    "alchemy": "catalog:",
    "jsdom": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4.54.0"
  }
}
